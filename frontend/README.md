# Frontend Scaffold

Bootstrap-based UI scaffold for FEMS Next.

The current frontend is static and intentionally not wired into the Docker Compose ports because Grafana uses port `3000`.
API calls should target the Flask backend at `http://localhost:5000`.
